[package]
name = "iron_engine"
version = "0.1.0"
edition = "2021"

[dependencies]
iron_table = { path = "../iron_table" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2.0"
chrono = "0.4"
regex = "1.12.3"
tracing = "0.1"

[dev-dependencies]
